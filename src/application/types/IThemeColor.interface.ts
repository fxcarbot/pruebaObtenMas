export interface IThemeColor {
  backgroundColor: string;
  themeBackgroundColor: string;
  lightIconActive: string;
  lightIconPassive: string;
  textColor: string;
  placeholderTextColor: string;
}
